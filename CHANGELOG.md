# 更新日志 CHANGELOG

## V0.5.0（2026-09-09）
- 新增「卡牌详情点按展开」：对局 / 揭晓 / 投影任意界面点按卡牌即弹出完整描述（角色、能力、缺陷、海克斯、环境、环境事件、胜利条件均覆盖）。
- 新增「双计时器」：环节配置可开启双计时，支持双方分开计时、任意加减、减到 0 封顶、单方重置 / 增减；撤销时正确回算剩余时间。
- 新增「环节配置（Timeline Builder）」：自由增减陈词 / 自由辩 / 质询 / 环境事件 / 海克斯选取环节数，拖拽调序；无判票时末环节显示「结束对局」。
- 引擎健壮性：修复海克斯 `requirements.value` 写成单字符串导致 `.some()` 抛错、弹窗不出现的问题；旧标签遭遇数据库版本升级时不再静默丢失持久化。
- 全量回归：真实浏览器（Edge）复现 + 引擎断言套件 101 项全绿。

## V0.4.x（前期迭代）
- V0.4.5.0：媒体 Blob 存储、导入导出稳定性。
- V0.4.4.0：海克斯需求 / 不兼容归一化，导入边界容错。
- V0.4.3.0：内容库可编辑、批量导入模板。
- V0.4.2.x：IndexedDB 迁移映射、BroadcastChannel 多端同步。

## V0.1 – V0.3（奠基）
- V0.3.x：稳定比赛引擎（秘密选择、揭晓、环境事件、判票）。
- V0.2：题面大厅与基础 UI。
- V0.1：原型。
